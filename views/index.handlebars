<img src="/assets/img/bigkauna.png">

<h1>Hamburgers: the cornerstone of any nutritious breakfast</h1>

<h2>That's a tasty burger!</h2>

<ul>
  {{#each burgers}}
    {{#unless devoured}}
      {{> burgers/burger-block devour=true}}
    {{/unless}}
  {{/each}}
</ul>

<h2>Open To 11pm - Drive Thru Til 12</h2>

<ul>
  {{#each burgers}}
    {{#if devoured}}
      {{> burgers/burger-block devour=false}}
    {{/if}}
  {{/each}}
</ul>

<h2>What's your Tasty Burger?!</h2>
<form class="create-form">

  <div class="form-group">
    <label for="bu">Burger Name:</label>
    <input type="text" id="bu" name="burger_name">
  </div>

  <div class="form-group">
    <label for="devoured">Devoured?</label><br>
    <input type="radio" name="devoured" value="1" checked> Devour it!<br>
  </div>

  <button type="submit">Add Burger</button>
</form>
